services:
  - type: web
    name: ai-text-detector
    runtime: python
    plan: free
    buildCommand: pip install --upgrade pip && pip install -r requirements_fastapi.txt
    startCommand: uvicorn fastapi_app:app --host 0.0.0.0 --port $PORT